# Placeholder assets

`icon.png`, `adaptive-icon.png`, `splash.png`, `favicon.png` are **functional placeholders** — a flat dark background (matching `theme.ts`'s `COLORS.bg`) with a plain "S" monogram in the app's purple accent, generated programmatically so the app can actually build (it couldn't before — these files didn't exist at all and `app.json` referenced them).

Not real design work. Replace with real brand assets before any Play Store / App Store submission.
